<div class="fwmb-brush-setting">
    <label>{{localize "FILRODENSWMB.UI.BrushSize"}}</label>
    <div class="fwmb-slider-group">
        <input type="range" name="brushSize" value="{{lookup uiState 'brushSize'}}" min="2" max="100" step="1" />
        <output>{{lookup uiState "brushSize"}}</output>
    </div>
</div>